// estamos dentro do arquivo src/types.ts tipando um vídeo do youtube

export type Users = {
    id: string,
    name: string,
    email: string,
    password: string,
    
  }

  export type Products = {
    id: string;
    name: string;
    price: string;
    description: string;
    imageUrl: string;
  }